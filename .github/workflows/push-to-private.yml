name: Push copy to private repo

permissions: write-all

on: [push]

jobs:
  push-to-repo:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v5
      with:
        token: ${{ secrets.TEMPLATE_PRIVATE_REPO }}

    - name: Push to private paper repository
      run: |
        git remote add private https://tobiasgrosser:${{ secrets.TEMPLATE_PRIVATE_REPO }}@github.com/opencompl/paper-template-private.git
        git push -f private HEAD:main
